syntax = "proto3";

import "common/model.proto";

package main;

// Сервис App
service FileAppService {
  // Получение статуса сервиса
  rpc GetStatus (common.Empty) returns (common.StatusResponse) {}
}
