syntax = "proto3";

package common;

message Empty {}

message StatusResponse {
  bool status = 1;
}

message FilterOffset {
  optional int32 count = 1;
  optional int32 page = 2;
}

enum SortOrders {
  ASC = 0;
  DESC = 1;
}
